apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: image-resolution-failure
  namespace: knative-map-test
spec:
  template:
    spec:
      containers:
        # The reserved .invalid top-level domain makes image resolution fail deterministically.
        - image: registry.invalid/headlamp-knative/missing:latest
