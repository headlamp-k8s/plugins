node_modules/
dist/
*.log
.DS_Store
*.tgz
.vscode/
.idea/
~

# Keep source code in git but exclude from npm
# (source code will be in git repo but not in published package)
*.tar.gz

# Extracted plugin directories (from npm run package + tar extraction)
# strimzi-headlamp/ - created when extracting tarball directly in project root
# plugins/ - created when extracting tarball for Kubernetes deployment (see README Method 4)
strimzi-headlamp/
plugins/
