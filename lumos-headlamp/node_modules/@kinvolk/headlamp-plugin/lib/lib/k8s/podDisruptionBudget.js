import { KubeObject } from './KubeObject';
class PDB extends KubeObject {
    get spec() {
        return this.jsonData.spec;
    }
    get status() {
        return this.jsonData.status;
    }
    get selectors() {
        const selectors = [];
        Object.keys(this.spec.selector.matchLabels).forEach(key => {
            selectors.push(`${key}: ${this.spec.selector.matchLabels[key]}`);
        });
        return selectors;
    }
}
PDB.kind = 'PodDisruptionBudget';
PDB.apiName = 'poddisruptionbudgets';
PDB.apiVersion = 'policy/v1';
PDB.isNamespaced = true;
export default PDB;
