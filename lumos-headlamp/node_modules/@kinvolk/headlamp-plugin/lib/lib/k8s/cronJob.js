import { KubeObject } from './KubeObject';
class CronJob extends KubeObject {
    get spec() {
        return this.getValue('spec');
    }
    get status() {
        return this.getValue('status');
    }
    getContainers() {
        return this.spec.jobTemplate?.spec?.template?.spec?.containers || [];
    }
}
CronJob.kind = 'CronJob';
CronJob.apiName = 'cronjobs';
CronJob.apiVersion = ['batch/v1', 'batch/v1beta1'];
CronJob.isNamespaced = true;
export default CronJob;
