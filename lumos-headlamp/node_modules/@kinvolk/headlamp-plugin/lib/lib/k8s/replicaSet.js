import { KubeObject } from './KubeObject';
class ReplicaSet extends KubeObject {
    get spec() {
        return this.jsonData.spec;
    }
    get status() {
        return this.jsonData.status;
    }
    getContainers() {
        return this.spec?.template?.spec?.containers || [];
    }
    getMatchLabelsList() {
        const labels = this.spec.selector.matchLabels || {};
        return Object.keys(labels).map(key => `${key}=${labels[key]}`);
    }
}
ReplicaSet.kind = 'ReplicaSet';
ReplicaSet.apiName = 'replicasets';
ReplicaSet.apiVersion = 'apps/v1';
ReplicaSet.isNamespaced = true;
export default ReplicaSet;
