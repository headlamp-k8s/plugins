import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { LightTooltip } from './Tooltip';
export default function LabelListItem(props) {
    const { labels = [] } = props;
    const [text, tooltip] = React.useMemo(() => {
        const text = labels.join(', ');
        const tooltip = labels.join('\n');
        return [text, tooltip];
    }, [labels]);
    if (!text) {
        return null;
    }
    return (_jsx(LightTooltip, { title: tooltip, interactive: true, children: _jsx("span", { children: text }) }));
}
