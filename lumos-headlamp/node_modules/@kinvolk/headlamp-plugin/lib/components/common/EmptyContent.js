import { jsx as _jsx } from "react/jsx-runtime";
import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';
import React from 'react';
export default function Empty({ color = 'textSecondary', children }) {
    return (_jsx(Box, { padding: 2, children: React.Children.map(children, child => {
            if (typeof child === 'string') {
                return (_jsx(Typography, { color: color, align: "center", children: child }));
            }
            return child;
        }) }));
}
