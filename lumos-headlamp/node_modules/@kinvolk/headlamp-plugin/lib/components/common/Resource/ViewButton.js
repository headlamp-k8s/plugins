import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import React from 'react';
import { useTranslation } from 'react-i18next';
import ActionButton from '../ActionButton';
import EditorDialog from './EditorDialog';
function ViewButton({ item, buttonStyle, initialToggle = false }) {
    const [toggle, setToggle] = React.useState(initialToggle);
    const { t } = useTranslation();
    return (_jsxs(_Fragment, { children: [_jsx(ActionButton, { description: t('translation|View YAML'), buttonStyle: buttonStyle, onClick: () => {
                    setToggle(true);
                }, icon: "mdi:eye", edge: "end" }), _jsx(EditorDialog, { item: item.jsonData, open: toggle, onClose: () => setToggle(toggle => !toggle), onSave: null })] }));
}
export default ViewButton;
