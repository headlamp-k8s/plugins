import { jsx as _jsx } from "react/jsx-runtime";
import { styled } from '@mui/system';
const SizedTextarea = styled('textarea')({
    width: '100%',
    minHeight: '40vh',
});
/** A very basic code editor. */
function SimpleEditor({ language, value, onChange }) {
    // TextareaAutosize doesn't react well within a dialog/tab
    return (_jsx(SizedTextarea, { "aria-label": `${language} Code`, onChange: event => onChange(event.target.value, event), value: value, spellCheck: "false" }));
}
export default SimpleEditor;
