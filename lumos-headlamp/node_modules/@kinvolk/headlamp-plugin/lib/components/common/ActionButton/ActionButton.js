import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Icon } from '@iconify/react';
import { ListItemIcon, ListItemText, MenuItem } from '@mui/material';
import IconButton from '@mui/material/IconButton';
import Tooltip from '@mui/material/Tooltip';
/**
 * To be used for our Action buttons.
 *
 * So we implement them consistently and encapsulate the implementation.
 */
export default function ActionButton({ description, longDescription, icon, onClick, color, width, edge, iconButtonProps, iconProps, buttonStyle = 'action', }) {
    if (buttonStyle === 'menu') {
        return (_jsxs(MenuItem, { onClick: onClick, children: [_jsx(ListItemIcon, { children: _jsx(Icon, { icon: icon, color: color, width: width, ...iconProps }) }), _jsx(ListItemText, { children: longDescription || description })] }));
    }
    return (_jsx(Tooltip, { title: longDescription || description, children: _jsx(IconButton, { "aria-label": description, onClick: onClick, edge: edge, size: "medium", ...iconButtonProps, children: _jsx(Icon, { icon: icon, color: color, width: width, ...iconProps }) }) }));
}
