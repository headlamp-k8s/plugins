import { jsx as _jsx } from "react/jsx-runtime";
import { useTranslation } from 'react-i18next';
import { LimitRange } from '../../lib/k8s/limitRange';
import ResourceListView from '../common/Resource/ResourceListView';
export function LimitRangeRenderer(props) {
    const { limitRanges, error, hideColumns = [], reflectTableInURL = 'limitranges', noNamespaceFilter, } = props;
    const { t } = useTranslation(['glossary', 'translation']);
    return (_jsx(ResourceListView, { title: t('glossary|LimitRange'), columns: ['name', 'namespace', 'cluster', 'age'], hideColumns: hideColumns, headerProps: {
            noNamespaceFilter,
        }, errorMessage: LimitRange.getErrorMessage(error), data: limitRanges, reflectInURL: reflectTableInURL, id: "headlamp-limitranges" }));
}
export function LimitRangeList() {
    const [limitRanges, error] = LimitRange.useList();
    return _jsx(LimitRangeRenderer, { limitRanges: limitRanges, error: error, reflectTableInURL: true });
}
