import { jsx as _jsx } from "react/jsx-runtime";
import { useTranslation } from 'react-i18next';
import { useParams } from 'react-router';
import { Lease } from '../../lib/k8s/lease';
import { DateLabel, DetailsGrid } from '../common';
export function LeaseDetails(props) {
    const params = useParams();
    const { name = params.name, namespace = params.namespace } = props;
    const { t } = useTranslation();
    return (_jsx(DetailsGrid, { resourceType: Lease, name: name, namespace: namespace, withEvents: true, extraInfo: item => item && [
            {
                name: t('Holder Identity'),
                value: item.spec.holderIdentity,
            },
            {
                name: t('Lease Duration Seconds'),
                value: item.spec.leaseDurationSeconds,
            },
            {
                name: t('Renew Time'),
                value: _jsx(DateLabel, { date: item.spec.renewTime }),
            },
        ] }));
}
