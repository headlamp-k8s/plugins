import { jsx as _jsx } from "react/jsx-runtime";
import { StatusLabel } from '../common/Label';
export function StatusLabelByPhase(phase) {
    return (_jsx(StatusLabel, { status: phase === 'Bound' ? 'success' : phase === 'available' ? 'warning' : 'error', children: phase }));
}
