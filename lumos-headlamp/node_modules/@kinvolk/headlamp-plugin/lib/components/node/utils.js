import { jsx as _jsx } from "react/jsx-runtime";
import { Box, Chip, Tooltip } from '@mui/material';
import { styled } from '@mui/system';
const WrappingBox = styled(Box)(({ theme }) => ({
    display: 'flex',
    justifyContent: 'left',
    flexWrap: 'wrap',
    '& > *': {
        margin: theme.spacing(0.5),
    },
}));
const PaddedChip = styled(Chip)({
    paddingTop: '2px',
    paddingBottom: '2px',
});
export function NodeTaintsLabel(props) {
    const { node } = props;
    if (node.spec?.taints === undefined) {
        return _jsx(WrappingBox, {});
    }
    const limits = [];
    node.spec.taints.forEach(taint => {
        limits.push(_jsx(Tooltip, { title: `${taint.key}:${taint.effect}`, children: _jsx(PaddedChip, { label: `${taint.key}:${taint.effect}`, variant: "outlined", size: "small" }) }, taint.key));
    });
    return _jsx(WrappingBox, { children: limits });
}
