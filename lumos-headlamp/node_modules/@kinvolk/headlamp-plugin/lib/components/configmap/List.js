import { jsx as _jsx } from "react/jsx-runtime";
import { useTranslation } from 'react-i18next';
import ConfigMap from '../../lib/k8s/configMap';
import ResourceListView from '../common/Resource/ResourceListView';
export default function ConfigMapList() {
    const { t } = useTranslation(['glossary', 'translation']);
    return (_jsx(ResourceListView, { title: t('glossary|Config Maps'), resourceClass: ConfigMap, columns: [
            'name',
            'namespace',
            'cluster',
            {
                id: 'data',
                label: t('translation|Data'),
                getValue: (configmap) => Object.keys(configmap.data || {}).length || 0,
                gridTemplate: 0.5,
            },
            'age',
        ] }));
}
