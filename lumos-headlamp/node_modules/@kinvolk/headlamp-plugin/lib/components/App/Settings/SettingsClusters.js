import { jsx as _jsx } from "react/jsx-runtime";
import { useTranslation } from 'react-i18next';
import { useClustersConf } from '../../../lib/k8s';
import { Link, SectionBox, SimpleTable } from '../../common';
export default function SettingsClusters() {
    const clusterConf = useClustersConf();
    const { t } = useTranslation(['translation']);
    return (_jsx(SectionBox, { title: "Cluster Settings", children: _jsx(SimpleTable, { columns: [
                {
                    label: t('translation|Name'),
                    getter: cluster => (_jsx(Link, { routeName: "settingsCluster", params: { cluster: cluster.name }, children: cluster.name })),
                },
                {
                    label: t('translation|Server'),
                    datum: 'server',
                },
            ], data: Object.values(clusterConf || {}) }) }));
}
