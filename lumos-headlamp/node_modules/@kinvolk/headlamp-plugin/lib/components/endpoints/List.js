import { jsx as _jsx } from "react/jsx-runtime";
import { useTranslation } from 'react-i18next';
import Endpoints from '../../lib/k8s/endpoints';
import { useFilterFunc } from '../../lib/util';
import LabelListItem from '../common/LabelListItem';
import ResourceListView from '../common/Resource/ResourceListView';
export default function EndpointList() {
    const { t } = useTranslation(['glossary', 'translation']);
    const filterFunc = useFilterFunc([
        '.jsonData.subsets[*].addresses[*].ip',
        '.jsonData.subsets[*].ports[*].port',
        '.jsonData.subsets[*].ports[*].name',
    ]);
    return (_jsx(ResourceListView, { title: t('glossary|Endpoints'), resourceClass: Endpoints, filterFunction: filterFunc, columns: [
            'name',
            'namespace',
            'cluster',
            {
                id: 'addresses',
                label: t('translation|Addresses'),
                getValue: endpoint => endpoint.getAddresses().join(', '),
                render: endpoint => _jsx(LabelListItem, { labels: endpoint.getAddresses() }),
                gridTemplate: 1.5,
            },
            'age',
        ] }));
}
