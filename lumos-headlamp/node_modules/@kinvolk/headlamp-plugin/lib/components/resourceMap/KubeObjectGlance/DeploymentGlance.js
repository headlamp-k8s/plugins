import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import { Box } from '@mui/system';
import { useTranslation } from 'react-i18next';
import { StatusLabel } from '../../common';
export function DeploymentGlance({ deployment }) {
    const { t } = useTranslation();
    const { replicas, availableReplicas } = deployment.status;
    const pods = `${availableReplicas || 0}/${replicas || 0}`;
    return (_jsxs(Box, { display: "flex", gap: 1, alignItems: "center", mt: 2, flexWrap: "wrap", children: [_jsxs(StatusLabel, { status: "", children: [t('glossary|Pods'), ": ", pods] }), deployment.status.conditions.map((it) => (_jsx(StatusLabel, { status: "", children: it.type }, it.type)))] }, "deployment"));
}
