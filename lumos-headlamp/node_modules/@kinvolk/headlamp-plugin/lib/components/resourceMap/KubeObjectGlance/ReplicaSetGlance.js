import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import { Box } from '@mui/system';
import { useTranslation } from 'react-i18next';
import { StatusLabel } from '../../common';
export function ReplicaSetGlance({ set }) {
    const { t } = useTranslation();
    const ready = set.status?.readyReplicas || 0;
    const desired = set.spec?.replicas || 0;
    return (_jsx(Box, { display: "flex", gap: 1, alignItems: "center", mt: 2, flexWrap: "wrap", children: _jsxs(StatusLabel, { status: "", children: [t('glossary|Replicas'), ": ", ready, "/", desired] }) }, "sets"));
}
