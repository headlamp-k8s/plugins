apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: waypoint-sample
  namespace: default
  annotations:
    sidecar.istio.io/proxyImage: "kmesh-daemon:v1"
spec:
  gatewayClassName: kmesh-waypoint
  listeners:
  - name: mesh
    port: 15008
    protocol: HTTP
